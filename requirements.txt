# ============================================================
# NeurologiqueTWIN — Python Dependencies
# ============================================================
# Core
numpy>=1.23
pandas>=1.5
scikit-learn>=1.2
scipy>=1.10
matplotlib>=3.6
plotly>=5.13

# Deep Learning
tensorflow>=2.12          # ResNet-CBAM training & inference
# torch>=2.0              # Uncomment if using PyTorch variant
# torchvision>=0.15

# EEG / signal processing
PyWavelets>=1.4           # Wavelet denoising in EEGProcessor
pyts>=0.13                # GASF / MTF / RP (optional — pure-NumPy fallback built-in)

# Agent-based simulation
mesa>=2.1                 # Multi-agent simulation (NeuroCity)

# API backend
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# Streamlit legacy UIs (preserved from original project)
streamlit>=1.30

# Utilities
geopy>=2.3                # Geographic calculations (used by EMS routing)
requests>=2.28
pyyaml>=6.0
python-dotenv>=1.0

# Visualisation (simulation outputs + notebooks)
seaborn>=0.12
Pillow>=9.0

# Notebook tools
jupyterlab>=4.0
ipywidgets>=8.0
